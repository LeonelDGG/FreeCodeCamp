# Lesson 15: Use an id Attribute to Style an Element

Similar a las clases, tenemos un selector CSS para estilizar el elemento asociado por ese ID

~~~css
    #cat-photo-element {
        background-color: green;
    }
~~~
