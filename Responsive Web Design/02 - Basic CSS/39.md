# Lesson 39: Use a custom CSS Variable

Una vez que hemos creado la variable, podemos asignarle su valor a propiedades CSS tan solo haciendo lo siguiente:

~~~css
    background: var(--penguin-skin);
~~~